import React from 'react';
import { Label, LabeledRow } from '../UI';

const TotalTransactions = () => (
  <LabeledRow label="TOTAL TRANSACTIONS">
    <Label>24</Label>
  </LabeledRow>
);

export default TotalTransactions;
